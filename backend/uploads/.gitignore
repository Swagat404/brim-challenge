# Receipt uploads — runtime user uploads stay local;
# but the seeded demo receipts ship with the deploy so the demo
# always has working thumbnails.
*
!.gitignore
# Re-include directories so git descends into them
!*/
!**/*.png
!**/*.jpg
!**/*.jpeg
